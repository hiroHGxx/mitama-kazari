# UT結果ログ — 夜じたて / 2026-09-03（run n4v8）

| 項目 | 内容 |
|---|---|
| 対象 | 夜じたて — 『月蝕綺譚 -Luna Occulta-』二次創作の道具（ゲームではない）。札絵を端末寸法に額装して写真に保存するまでを受け持つ。対象の姿は SPEC.md §7.7「閉じた姿ひとつに」（1画面・状態はひとつ） |
| モード | hybrid（ユーザビリティ観察＋受入確認） |
| 環境 | ローカル配信 `http://127.0.0.1:<自動割当ポート>/`（観察スクリプトが charset 付きで配信）。**本番 origin では実施していない** |
| 画面幅 | 主 393×740（bar表示中・#size=1179x2556）、副 375×560（SE・#size=750x1334） |
| シナリオ | `docs/ut/scenarios/UTシナリオ_20260903_moderator.html`（13件） |
| 観察役 | 検品担当（結） |
| 参加者 | AI-P01（AI 代理） |

## この記録の制約（先に書く）

**参加者は人間ではない。**AI（検品担当）が初見の遊び手を代行し、puppeteer-core（Chrome実機バイナリ）で実際に操作した。したがって——

- **表示・数値・時間・コードは direct な観察**（実測値とスクリーンショット・JSONログが残っている）
- **「迷った」「分かりにくい」の類は inferred**（人間の詰まりを測ったものではない）。本UTでは無理に迷いを演じていない
- **発話引用（quote）は全件 null**
- **実機の指・共有シート・体感速度は原理的に演じられない**（C-9は not-tested として残す）
- **同一 browser context 内で各シナリオを順にページ遷移して実行したため、localStorage（`yo-jitate.pick`）が前のシナリオから引き継がれた。** B-5・B-6・B-7 の「開始時の表示」はこの持ち越しの影響を受けている。B-7 は元の一括計測でこの持ち越しにより `freshLoadMs` が計測不能（12秒でタイムアウト）になったため、`browser.createBrowserContext()` で完全に独立させた条件で測り直した（`obs-b7-recheck.json`）。B-4・E-11などコアの合否判定自体はこの持ち越しの影響を受けない
- 着手前に既存の機械検査 `scripts/check-layout.js` を単独実行し、**150項目すべて通ることを確認済み**（`baseline.md`）。本 run はその検査が担保していない「初見の理解・迷い・回復」を見る

1人（しかもAI代理）で起きたことは手がかりであって、発生頻度の根拠にはならない。ただし境界条件の追試（B-4の中央寄せ・B-7の計測環境の切り分け）は再現手順が明記された直接測定であり、そのまま使う。

## 集計

| 状態 | 件数 | シナリオ |
|---|---|---|
| 完了 / 受入OK | 10 | A-1 / A-2 / A-3 / B-5 / B-6 / B-7 / C-8 / D-10 / E-11 / E-12 |
| 摩擦あり完了 | 2 | B-4（受入ng）／E-13（受入 not-applicable） |
| skipped（not-tested・実機のみ） | 1 | C-9 |
| 失敗 / blocked | 0 | — |

（内訳の正は `results.jsonl` の13行）

## 各シナリオ

### A-1 — 完了 / 受入 OK

**観察**: URLを開いた直後（t=47ms）、地と蝕環のみが額に描かれ、周りの部品（h1・帯など）はopacity 0・pointer-events:noneで押せない状態だった（body.locked）。額の時計の帯の題字（#daimei）はt=343msまでにopacity 1に達した。t=1145msの時点でもbody.lockedはtrueのまま（押せない）。t=1443msでbody.lockedがfalseに変わり、pointer-events:autoかつsaveボタンの中心座標がdocument.elementFromPointで拾える状態（押せる）になった。同じt=1443msでh1のopacityは0.575892（上昇中）、#daimeiのopacityは0.0751588（消えかけ）。t=1942msでh1 opacity=1・#daimei opacity=0。t=2243msで#daimeiはdisplay:noneに変わっていた。

**発話**: —（AI代理のため無し）

**解釈**: 空欄（分析工程の持ち場）

**証拠**: `screenshots/A1-veiled-300ms.png`<br>`screenshots/A1-risen-1600ms.png`<br>`obs-all.json A1`

**タグ**: opening, timing, title-reveal

### A-2 — 完了 / 受入 OK

**観察**: 開いてt=418msでdocument.bodyへpointerdownイベントを発火させたところ、63ms後のt=481msの時点でh1のopacityは1（完全）・pointer-eventsはauto・saveボタンの中心座標がelementFromPointで拾えた（押せる状態）。

**発話**: —（AI代理のため無し）

**解釈**: 空欄（分析工程の持ち場）

**証拠**: `screenshots/A2-after-tap.png`<br>`obs-all.json A2`

**タグ**: opening, skip, timing

### A-3 — 完了 / 受入 OK

**観察**: iPhone SE相当（375×560・#size=750x1334）で開いたところ、frameはtop2〜bottom424・sheetはtop426〜bottom560（画面下端と一致）・saveはtop484〜bottom532（画面下端まで28px余り）。document.documentElement.scrollHeight=560・clientHeight=560で頁は縦にスクロールしない。本文中で最小の文字は12px（『咲耶』の表示）。pageerrorイベントは0件。

**発話**: —（AI代理のため無し）

**解釈**: 空欄（分析工程の持ち場）

**証拠**: `screenshots/A3-se-risen.png`<br>`obs-all.json A3`

**タグ**: small-device, SE, layout

### B-4 — 摩擦あり完了 / 受入 NG

**観察**: 開始時（初回・控え未設定）の名札は「咲耶」、#stripの里の境の隙間は4か所、ボタンは29個。列を最後尾までスクロールすると顔アイコンは29/29読み込み済み。列内で「シンラ」（根の国＝29柱中いちばん最後の1柱）のボタンをクリックすると、名札は「シンラ」に変わりaria-pressed=trueは1件だけになった。ただし選んだボタンの中心（viewport座標）と#strip容器の中心のずれは295.5pxで、中央には寄っていなかった。追試（obs-b4-recheck.json）: #stripのscrollHeight=2089px・clientHeight=660px・maxScrollTop=1429px。リスト中ほどの「カルマ」を選ぶとずれは0.5px（ほぼ完全に中央）。最後の「シンラ」を選ぶとscrollTopが1429（=maxScrollTop）に張り付き、ずれ295.5px。先頭の「咲耶」を選ぶとscrollTopが0に張り付き、ずれ295.5px（逆方向）。

**発話**: —（AI代理のため無し）

**解釈**: 空欄（分析工程の持ち場）

**証拠**: `screenshots/B4-strip-scrolled.png`<br>`screenshots/B4-after-select-shinra.png`<br>`screenshots/B4-recheck-boundary-shinra.png`<br>`obs-all.json B4`<br>`obs-b4-recheck.json`<br>`index.html:801-807 centerStrip()`

**タグ**: strip, scroll-into-view, boundary, centering

### B-5 — 完了 / 受入 OK

**観察**: この時点で名札はB-4から持ち越された「シンラ」表示だった（このrunでは各シナリオが同一browser context内の新しいページとして開くため、localStorageの控えが前のシナリオから引き継がれる。テスト環境固有の特性であり、対象アプリの挙動ではない）。名札のテキストは「シンラ しんら／根の国・土 名前で選ぶ ▾」で矢印記号を含んでいた。クリックすると#pickerがshow状態になった。#pickBody内には里の見出し（.pick-sato）が5つ、内容は「甲賀」「伊賀」「雑賀」「風魔」「根の国」の順で存在した（#stripには同じ里の境が隙間のみでテキストラベルは無い）。「オロチ」を選ぶと#pickerのshowクラスが外れ（自動的に閉じた）、名札は「オロチ」に変わった。

**発話**: —（AI代理のため無し）

**解釈**: 空欄（分析工程の持ち場）

**証拠**: `screenshots/B5-picker-open.png`<br>`obs-all.json B5`

**タグ**: picker, name-search, sato-labels

### B-6 — 完了 / 受入 OK

**観察**: 階梯の3段目をクリックすると、aria-pressed=trueのインデックスは3・テキストが入っているボタンは1個・style.backgroundColorが設定されているボタン（満ちている段）は3個だった。続けて8段目をクリックすると、インデックス8・テキスト表示1個・満ちているボタン8個だった。

**発話**: —（AI代理のため無し）

**解釈**: 空欄（分析工程の持ち場）

**証拠**: `screenshots/B6-depth-initial.png`<br>`screenshots/B6-depth-3.png`<br>`screenshots/B6-depth-8.png`<br>`obs-all.json B6`

**タグ**: depth, tap-select

### B-7 — 完了 / 受入 OK

**観察**: 階梯2段目のy座標から8段目のy座標までマウスで12ステップに分けて連続移動しmouseupすると、ドラッグ中間地点でaria-pressed=trueのボタンは8段目、mouseup後も8段目のままだった。元の一括実行では同一browser context内で直前のB-5/B-6の選択（御霊=オロチ）が持ち越されていたため、「新しい御霊への切り替え」として同じオロチを再度クリックする形になり、choose()内のid===curIdガードで実質何も起きず、time-to-enabled計測は12000msでタイムアウト（null）した。切り分けのため独立したbrowser context（obs-b7-recheck.json）で最初から測り直したところ、開始直後の控えは既定値（{id:'sakuya',depth:5}）であることを確認したうえで、未選択のオロチへの初回切り替えが405ms、3秒待って隣接する段への切り替えが383ms、さらに0.5秒待って反対側の端の段への切り替えが397msだった。3値の差は20ms程度で、この指標（saveボタンのdisabled解除までの時間）では先読みによる有意な高速化を検出できなかった。

**発話**: —（AI代理のため無し）

**解釈**: 空欄（分析工程の持ち場）

**証拠**: `screenshots/B7-mid-drag.png`<br>`screenshots/B7-recheck-isolated.png`<br>`obs-all.json B7`<br>`obs-b7-recheck.json`<br>`index.html:1079-1096 revealArt()`

**タグ**: depth, drag, prefetch, measurement-limit

### C-8 — 完了 / 受入 OK

**観察**: navigator.shareをAbortErrorでrejectするよう差し替えて#saveを押し400ms待っても、#tipsはshow状態にならなかった（出ない）。navigator.shareをresolveに差し替えて再度押すと、5000ms以内に#tipsがshowになった。navigator.canShareをfalseに差し替えて（download経路扱い）再度押しても、5000ms以内に#tipsがshowになった。#tipsBodyの正規化テキスト（空白除去）は#howtoTextの正規化テキストと完全一致した。

**発話**: —（AI代理のため無し）

**解釈**: 空欄（分析工程の持ち場）

**証拠**: `screenshots/C8-tips-card.png`<br>`obs-all.json C8`

**タグ**: save, share-sheet-stub, tips-card

### C-9 — skipped / 受入 skipped

**観察**: 実機のnavigator.shareは、ヘッドレスChromeでは解決も棄却もせず永久に待つ仕様のため、機械実行では原理的に検証できない。今回は実施していない（実機に回す）。

**発話**: —（AI代理のため無し）

**解釈**: 空欄（分析工程の持ち場）

**証拠**: —

**タグ**: not-tested, real-device-only, share-sheet

### D-10 — 完了 / 受入 OK

**観察**: ≡を押すと#menuがshow状態になった。#menuBody内に#dailyBox（hidden=false。この端末寸法1179×2556に合致するため表示された）・#copyUrl・#log・#howtoTextの各要素が存在した。#dailyUrlのテキストはhttps://github.com/hiroHGxx/yo-jitate/releases/download/daily/yo-jitate-1179x2556.jpg だった。#dailyBoxをopen=trueにしたうえで#copyUrlを押すと、差し替えたclipboard.writeTextが1回呼ばれ、書き込まれた文字列は上記URLと一致、ボタンのラベルは「写しました」に変わった。#logのテキストには「素材蔵からcrossOriginで読む 通った」「toBlobで書き出す 通った 159 KBのJPEG」「共有シートに画像を渡せるか 通った」の3行があった。脚注テキストに「二次創作」「原本を配ってはいません」の文言が含まれていた。

**発話**: —（AI代理のため無し）

**解釈**: 空欄（分析工程の持ち場）

**証拠**: `screenshots/D10-menu-open.png`<br>`obs-all.json D10`

**タグ**: menu, clipboard, howto, log

### E-11 — 完了 / 受入 OK

**観察**: 1つ目のページで「カルラ」を選び、階梯9段目を選んだ。localStorageのyo-jitate.pickは{id:'karura',depth:9}だった。同一browser context内で新しいページを開いて同URLを読み込むと、立ち上がり後の名札は「カルラ」・aria-pressed=trueの階梯は9段目で、直前に書いた控えと一致した。

**発話**: —（AI代理のため無し）

**解釈**: 空欄（分析工程の持ち場）

**証拠**: `screenshots/E11-restored.png`<br>`obs-all.json E11`

**タグ**: revisit, localStorage, restore

### E-12 — 完了 / 受入 OK

**観察**: window.localStorageへのアクセスが例外を投げるよう上書きした状態で開いたところ、30秒以内にsaveボタンのdisabled属性がfalseになった（最後まで立ち上がり、保存可能な状態に達した）。pageerrorイベントは0件だった。

**発話**: —（AI代理のため無し）

**解釈**: 空欄（分析工程の持ち場）

**証拠**: `screenshots/E12-no-localstorage.png`<br>`obs-all.json E12`

**タグ**: localStorage-unavailable, resilience

### E-13 — 摩擦あり完了 / 受入 対象外

**観察**: kura.vibe.co.jp宛のリクエストを全件abortする状態で開いたところ、開幕自体（body.lockedの解除）は絵の到着を待たない設計どおり通常どおり進んだ。2秒後の時点でsaveボタンはdisabledのまま、#frameに'loading'クラスは付いておらず、.waitingの実効displayは'none'（「顕れています…」の文言は画面上どこにも表示されない）。#logには「素材蔵からcrossOriginで読む 落ちた／読み込めない: https://kura.vibe.co.jp/fuda/v2/fuda_karura_09.webp」の1行だけが記録されていた。9秒後（5秒の保険＝WAIT_NOTICEを跨いだ後）も状態は一切変化なし（save disabled・loading無し・waiting非表示・ログ同一）。ブロックしたリクエストは1件のみ（現在選択中の1枚の初回読み込み分）で、これはloadFuda()の失敗がcatchブランチに落ち、成功時のみ呼ばれるprefetchRest()が一度も呼ばれなかったため（コード確認: index.html refresh()内、prefetchRestの呼び出しは.then()節の中のみ）。pageerrorは0件（JS例外としては表面化しない、静かな失敗）。

**発話**: —（AI代理のため無し）

**解釈**: 空欄（分析工程の持ち場）

**証拠**: `screenshots/E13-2s.png`<br>`screenshots/E13-9s.png`<br>`obs-all.json E13`<br>`index.html:1231-1252 refresh()`

**タグ**: network-failure, asset-vault-unreachable, silent-failure

